This directory previously contained an incomplete Next.js frontend scaffold.

Current production UI is served by:
- `app/templates/index.html`
- `app/static/app.js`
- `app/static/style.css`

If you rebuild a React/Next frontend, reintroduce a valid `package.json` and
wire API calls to the FastAPI endpoints under `/api/*`.
